version: "3.9"
services:
  app:
    build:
      dockerfile: 'docker/Dockerfile'
      context: '..'
      target: dev
    ports:
      - "8080:8080"
    restart: always
    volumes:
      - '..:/app'
    environment:
      NODE_ENV: development

networks:
  app:
    driver: bridge